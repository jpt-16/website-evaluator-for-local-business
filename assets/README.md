Drop the before/after mockup images here, named exactly:

- `mockup-before.png`
- `mockup-after.png`

Both `index.html` (the live checker) and `report-template.html` (the
curated `/reports/<slug>` pages) already reference these two filenames —
nothing else needs to change once they're uploaded.
